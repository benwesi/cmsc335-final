<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Courses - Grade Calculator</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header>
        <nav class="container">
            <a href="/" class="logo">Grade Calculator</a>
            <ul class="nav-links">
                <li><a href="/dashboard">Dashboard</a></li>
                <li><a href="/courses">My Courses</a></li>
                <li><a href="/logout">Logout</a></li>
            </ul>
        </nav>
    </header>

    <main class="container">
        <div class="page-header">
            <div>
                <h1 class="page-title">My Courses</h1>
            </div>
            <div class="page-actions">
                <a href="/courses/add" class="btn btn-primary">Add New Course</a>
            </div>
        </div>

        <% if (courses.length > 0) { %>
            <div class="grid grid-2">
                <% courses.forEach(course => { %>
                    <div class="card">
                        <div class="card-header">
                            <h3 class="card-title"><%= course.name %></h3>
                        </div>
                        <div>
                            <p><strong>Course Code:</strong> <%= course.code %></p>
                            <p><strong>Credits:</strong> <%= course.credits %></p>
                            <p><strong>Semester:</strong> <%= course.semester %> <%= course.year %></p>
                        </div>
                        <div class="mt-3" style="display: flex; gap: 0.5rem;">
                            <a href="/courses/<%= course._id %>" class="btn btn-sm btn-primary">View Details</a>
                            <a href="/assignments/add/<%= course._id %>" class="btn btn-sm btn-secondary">Add Assignment</a>
                            <a href="/assignments/calculator/<%= course._id %>" class="btn btn-sm btn-success">Calculator</a>
                            <form action="/courses/<%= course._id %>/delete" method="POST" style="display: inline;" 
                                  onsubmit="return confirm('Are you sure you want to delete this course and all its assignments?')">
                                <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                            </form>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <div class="card">
                <div style="text-align: center; padding: 3rem 2rem;">
                    <div style="font-size: 48px; color: var(--gray-300); margin-bottom: 1rem;">📚</div>
                    <h2 style="color: var(--gray-700); margin-bottom: 1rem;">No Courses Yet</h2>
                    <p style="color: var(--gray-500); margin-bottom: 2rem;">Start tracking your grades by adding your first course!</p>
                    <a href="/courses/add" class="btn btn-primary">Add Your First Course</a>
                </div>
            </div>
        <% } %>
    </main>

    <footer>
        <div class="container">
            <p>&copy; 2025 Grade Calculator. Built for CMSC335 Final Project.</p>
        </div>
    </footer>
</body>
</html>